# c-chess
中国象棋训练程序

## 启动
在仓库根目录执行：

```bash
npm start
```

打开：

`http://localhost:3001`

局域网辅助入口：

- `http://localhost:3001/lan` 查看当前可用的局域网地址清单（含 `hostname.local`）
- `http://localhost:3001/go` 自动跳转到优先地址（mDNS/局域网 IP）
